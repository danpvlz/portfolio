---
interface Props {
    title: string;
}
const { title } = Astro.props;
---

<span class="flex items-end gap-2 mb-8">
  <h1 class="text-5xl md:text-7xl font-extrabold">{title}</h1>
  <span class="text-2xl md:text-4xl leading-tight">●</span>
</span>
